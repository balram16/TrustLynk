{
  "version": 2,
  "framework": "nextjs",
  "installCommand": "cd frontend && pnpm install --no-frozen-lockfile || npm install --legacy-peer-deps",
  "buildCommand": "cd frontend && pnpm build || npm run build",
  "outputDirectory": "frontend/.next",
  "env": {
    "NEXT_TELEMETRY_DISABLED": "1"
  }
}


